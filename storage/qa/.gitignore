# Generated at runtime. May contain local IDs, credentials, or API tokens.

# Newman captures, QA reports, local scan output
/reports/

# Disposable IDs from last QA seed run
qa-manifest.json

# Real Postman env (password + token). Example template is NOT ignored.
postman/*environment.json

# Temporary QA exports
tmp/
